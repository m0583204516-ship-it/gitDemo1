A user management web application that allows creating multiple users and managing personal tasks for each one.  
Each user has their own task list, with the ability to add and delete tasks independently.  
The system displays tasks based on the currently active user and updates the interface dynamically.
